import { BrainstormTopicPost } from './brainstorm-topic-post';

describe('BrainstormTopicPost', () => {
  it('should create an instance', () => {
    expect(new BrainstormTopicPost()).toBeTruthy();
  });
});

import { ProjectBrainstormPost } from './project-brainstorm-post';

describe('ProjectBrainstormPost', () => {
  it('should create an instance', () => {
    expect(new ProjectBrainstormPost()).toBeTruthy();
  });
});

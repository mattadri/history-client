import { BrainstormUserPost } from './brainstorm-user-post';

describe('BrainstormUserPost', () => {
  it('should create an instance', () => {
    expect(new BrainstormUserPost()).toBeTruthy();
  });
});

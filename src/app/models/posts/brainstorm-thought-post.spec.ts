import { BrainstormThoughtPost } from './brainstorm-thought-post';

describe('BrainstormThoughtPost', () => {
  it('should create an instance', () => {
    expect(new BrainstormThoughtPost()).toBeTruthy();
  });
});

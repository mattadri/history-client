import { TimelinePersonPost } from './timeline-person-post';

describe('TimelinePersonPost', () => {
  it('should create an instance', () => {
    expect(new TimelinePersonPost()).toBeTruthy();
  });
});

import { BrainstormResponse } from './brainstorm-response';

describe('BrainstormResponse', () => {
  it('should create an instance', () => {
    expect(new BrainstormResponse()).toBeTruthy();
  });
});

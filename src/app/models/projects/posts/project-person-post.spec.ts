import { ProjectPersonPost } from './project-person-post';

describe('ProjectPersonPost', () => {
  it('should create an instance', () => {
    expect(new ProjectPersonPost()).toBeTruthy();
  });
});

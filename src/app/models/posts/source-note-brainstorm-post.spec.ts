import { SourceNoteBrainstormPost } from './source-note-brainstorm-post';

describe('SourceNoteBrainstormPost', () => {
  it('should create an instance', () => {
    expect(new SourceNoteBrainstormPost()).toBeTruthy();
  });
});

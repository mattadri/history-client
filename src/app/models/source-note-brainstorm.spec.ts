import { SourceNoteBrainstorm } from './source-note-brainstorm';

describe('SourceNoteBrainstorm', () => {
  it('should create an instance', () => {
    expect(new SourceNoteBrainstorm()).toBeTruthy();
  });
});

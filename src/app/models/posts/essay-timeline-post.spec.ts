import { EssayTimelinePost } from './essay-timeline-post';

describe('EssayTimelinePost', () => {
  it('should create an instance', () => {
    expect(new EssayTimelinePost()).toBeTruthy();
  });
});

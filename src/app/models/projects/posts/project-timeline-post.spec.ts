import { ProjectTimelinePost } from './project-timeline-post';

describe('ProjectTimelinePost', () => {
  it('should create an instance', () => {
    expect(new ProjectTimelinePost()).toBeTruthy();
  });
});

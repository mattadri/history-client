import { TimelineEventPost } from './timeline-event-post';

describe('TimelineEventPost', () => {
  it('should create an instance', () => {
    expect(new TimelineEventPost()).toBeTruthy();
  });
});

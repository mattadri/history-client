import { ChartDatasetDataPost } from './chart-dataset-data-post';

describe('ChartDatasetDataPost', () => {
  it('should create an instance', () => {
    expect(new ChartDatasetDataPost()).toBeTruthy();
  });
});
